# Server Configuration
# Port number for the Express server (default: 3850)
# Valid range: 1-65535
PORT=3850

# Node environment mode
# Valid values: development, production, test
# Default: development
NODE_ENV=development

# Logging Configuration
# Log level for Morgan and application logging
# Valid values: error, warn, info, debug
# Default: info
LOG_LEVEL=info

# WebSocket Authentication
# REQUIRED in production, optional in development
# Generate secure token: openssl rand -hex 32
# Minimum length: 16 characters
WS_AUTH_TOKEN=dev-token-12345

# Docker Container Credential Paths
# These paths are mounted into containers for Claude CLI authentication
# CRITICAL SECURITY: These are mounted read-only to prevent credential modification

# Claude account directory containing OAuth tokens and config
# Default: $HOME/.claude-acc4
# Contains: .claude.json (OAuth tokens), .claude/ (config)
CLAUDE_ACCOUNT_DIR=/Users/kostasnoreika/.claude-acc4

# Claude Manager scripts directory
# Default: $HOME/.claude-manager
# Contains: 'c' alias scripts and utilities
# Note: Mounted read-write as it needs to write logs/cache
CLAUDE_MANAGER_DIR=/Users/kostasnoreika/.claude-manager

# MCP (Model Context Protocol) configuration directory
# Default: /opt/mcp
# Contains: Shared MCP server configurations
# Mounted read-only as containers should not modify shared configs
MCP_DIR=/opt/mcp

# SECURITY FIX (HIGH-001): Allowed Workspace Paths
# Comma-separated list of allowed workspace directories for containers
# Only paths starting with these prefixes are allowed to be mounted
# Default: /opt/dev,/opt/prod
# Examples:
#   - /opt/dev,/opt/prod (default)
#   - /home/user/projects,/var/www
#   - /Users/username/workspace (macOS single path)
ALLOWED_WORKSPACE_PATHS=/opt/dev,/opt/prod

# Future Environment Variables (for reference, not yet implemented)
# Uncomment and configure when implementing these features:

# Database Configuration
# DATABASE_URL=postgresql://localhost:5432/claude_studio
# DATABASE_MAX_CONNECTIONS=20
# DATABASE_IDLE_TIMEOUT=30000

# Session Management
# SESSION_SECRET=your-secure-random-secret-key-here
# SESSION_MAX_AGE=86400000

# Docker Configuration
# Docker daemon connection URL
# Development: unix:///var/run/docker.sock (default, can omit)
# Production: tcp://docker-proxy:2375 (set in docker-compose.prod.yml)
# Supports both Unix socket and TCP connections
# Examples:
#   DOCKER_HOST=unix:///var/run/docker.sock (Unix socket - default)
#   DOCKER_HOST=/var/run/docker.sock (Unix socket - shorthand)
#   DOCKER_HOST=tcp://docker-proxy:2375 (TCP proxy - production)
#   DOCKER_HOST=tcp://localhost:2375 (TCP - local testing)
# DOCKER_HOST=unix:///var/run/docker.sock
# DOCKER_IMAGE=claude-studio-env:latest

# API Keys (when implementing external services)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# CORS Configuration (default: http://localhost:3850)
# CORS_ORIGIN=http://localhost:3850
